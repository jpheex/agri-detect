services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATA_DIR=/data
      - PORT=8000
    volumes:
      - agri_data:/data

volumes:
  agri_data:
